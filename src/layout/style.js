import styled from '@emotion/styled'

export const LayoutWrapper = styled.div`
  position: relative;
  max-height: 100vh;

  .layout-content {
    padding: 0px calc(var(--space-md) * 3) var(--space-md)
      calc(var(--space-md) * 3);
  }
`
