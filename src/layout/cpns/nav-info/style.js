import { css } from '@emotion/react'

export const navInfoCss = css`
  margin-right: var(--space-xl);
  color: var(--text-primary);
  .info-name {
    color: var(--text-primary);
    margin-left: calc(var(--space-xs) - 2px);
  }
`
